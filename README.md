# Live Orbit Website

Launch website for Live Orbit, a prelaunch iPhone app for live satellites, visible passes, Sky Mode, insights, and space news.

## Commands

```bash
npm install
npm run lint
npm run typecheck
npm run build
```

The site is a Next.js static export. GitHub Pages deploys the `out` directory through `.github/workflows/deploy.yml`.
